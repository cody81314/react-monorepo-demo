# mahjong-logic

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mahjong-logic` to build the library.

## Running unit tests

Run `nx test mahjong-logic` to execute the unit tests via [Jest](https://jestjs.io).
